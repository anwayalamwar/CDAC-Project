apiVersion: v1
kind: Service

metadata:
  name: todo-service
  namespace: secureflow

spec:
  selector:
    app: todo-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080

  type: NodePort
